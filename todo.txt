Do not send message if source and dest are same (done)
Cache reachability results (basic, no invalidation, only caching positive
results)
Make system independent of localhost (done)

Change usage of batch_requests from regular pointers to shared_ptr.
Make new threads for handling each new reach request (done at each server,
pending at central server) - need to change to thread pool (done).

Add update functions.

Make edge have only one meta_element. No need to store local information.
Examine code for spurious copying/mallocing.
Add error message printing on msg packing/unpacking.
Make allowance for multiple reach_nodes in reachability requests.
Try to get rid of PO6_NDEBUG_LEAKS.
Check memory leaks using valgrind.
Do code profiling to identify bottleneck functions.
